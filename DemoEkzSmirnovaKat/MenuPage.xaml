<Page x:Class="DemoEkzSmirnovaKat.MenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DemoEkzSmirnovaKat"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MenuPage">

    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button x:Name="postavchikiButton"  Content="Поставщики" Style="{StaticResource btn}"/>
            <Button x:Name="klientsButton" Content="Клиенты" Style="{StaticResource btn}"/>
            <Button x:Name="ModelAvtoButton" Content="Модель авто" Style="{StaticResource btn}"/>
            <Button x:Name="sdelkaButton" Content="Сделка" Style="{StaticResource btn}"/>

            </StackPanel>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button x:Name="NacenkaButton" Content="Наценка" Style="{StaticResource btn}"/>
                <Button x:Name="preickurantButton" Content="Прейскурант" Style="{StaticResource btn}"/>
            </StackPanel>

        </StackPanel>

    </Grid>
</Page>
